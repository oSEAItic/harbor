{
  "id": "harbor",
  "name": "Harbor Memory",
  "description": "Cross-session curated memory, cross-device sync, and knowledge graph for OpenClaw agents",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "autoSync": {
        "type": "boolean",
        "description": "Write Harbor context to workspace on session start",
        "default": true
      },
      "autoCapture": {
        "type": "boolean",
        "description": "Capture context before compaction via harbor remember",
        "default": true
      },
      "contextFile": {
        "type": "string",
        "description": "Workspace file to write Harbor context into",
        "default": "memory/harbor-context.md"
      }
    }
  }
}
