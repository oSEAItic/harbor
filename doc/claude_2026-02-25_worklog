## 2026-02-25 — Phase 3A 验证 + Proxy 凭证管理

### 概述

本次工作分两个阶段：

1. **Phase 3A 端到端验证** — 从测试到真实 Claude Code 环境，证明 Harbor 的 agent-driven schema learning 确实能用
2. **Proxy 凭证管理** — 为 proxy 模式添加 OS keychain 凭证注入，解决第三方 MCP 服务器的 API key 安全问题

核心发现：Agent 不会自动响应工具结果里的 hint 文本（把它当数据），但**工具描述**里的指令性语言有效。修复后 Claude Code 在真实环境中成功完成了完整的 schema 学习循环。

---

### Phase 3A: 端到端验证

#### A. E2E 测试 (`internal/proxy/e2e_test.go`)

用真实 mock 数据（GitHub issues ~4500 bytes, filesystem listing ~2000 bytes）测试完整 proxy 流程：

| 测试场景 | 结果 |
|----------|------|
| 首次调用 → raw + hint | ✅ hint 包含自动检测的字段建议 |
| Agent 调用 learn_schema | ✅ schema 存储、版本化 |
| 第二次调用 → 压缩输出 | ✅ GitHub 81%, Filesystem 67% 减少 |
| 第三次调用 → memory hit | ✅ <200µs 延迟，无 upstream 调用 |
| 来源追踪 (provenance) | ✅ connector/resource/source/timestamp/params |
| 数据新鲜度 (freshness) | ✅ TTL 检查、过期检测 |
| 多源召回 (multi-source) | ✅ 跨工具 recall 返回所有来源 |

#### B. LLM 质量测试 (`internal/proxy/quality_test.go`)

用真实 OpenAI API (gpt-4o-mini) 对比 raw vs Harbor 结构化数据的 agent 表现：

| 测试维度 | 发现 |
|----------|------|
| Context Focus | 两者都能识别关键 issue，但 Harbor 用 83% 更少的 input tokens (253 vs 1492) |
| Attribution | Harbor: agent 能引用 "Source 1, list_issues" + 时间戳；Raw: 只说 "Data set 1" |
| Freshness Awareness | **关键发现** — Harbor 让 agent 识别到过期数据并标注 "may not be accurate"；Raw agent 完全无法感知数据新鲜度 |
| Signal-to-Noise | 结构性测试 — Raw 55% 信号(24 噪声字段)，Harbor 100% 信号(0 噪声字段) |

#### C. 真实 Claude Code 验证

1. 配置 `.mcp.json` 使 Harbor proxy 包装 filesystem MCP server
2. **第一次测试失败** — Claude 看到 `[Harbor: No compression schema...]` hint 但忽略了它，直接回答用户问题
3. **修复 hint 策略**:
   - `proxy.go`: 将 `harbor_learn_schema` 工具描述改为 `"IMPORTANT: You MUST call this tool whenever a tool response contains '[Harbor:' at the end."`
   - `handler.go`: 新增 `buildLearnHint()` 自动检测 JSON 字段并给出具体建议
   - `handler.go`: 新增 `detectTopFields()` 从 JSON 提取有用字段名，过滤 URL 噪声
4. **第二次测试成功** — Claude 自动调用 `harbor_learn_schema`，选择 fields: `["name", "type"]`，template: `"[{type}] {name}"`
5. **第三次测试确认** — 后续调用返回压缩/缓存数据，无 hint

**结论**：Agent 把工具结果当数据，但把工具描述当指令。tool description 是控制 agent 行为的正确通道。

---

### Proxy 凭证管理

#### 问题

第三方 MCP 服务器（GitHub, Notion, Slack 等）需要 API key，当前做法：
- 在 `.mcp.json` 的 `env` 字段里明文写 token
- 或在 shell profile 里 export 环境变量
- `proxy.go` 用 `os.Environ()` 全透传 — 所有 MCP server 能看到所有环境变量

Harbor 的 connector 模式已有完整的 keychain 集成 + 环境隔离，但 proxy 模式完全没有。

#### 实现

**改动文件**:

| 文件 | 改动 |
|------|------|
| `internal/proxy/proxy.go` | +Config.Credentials 字段, +buildProxyEnv() 函数, 替换 os.Environ() |
| `internal/cli/proxy.go` | +extractCredentialFlags() 手动解析 --credential flag, 更新帮助文本 |
| `internal/proxy/credential_test.go` | 新增: env override 逻辑测试 |
| `internal/cli/proxy_test.go` | 新增: flag 解析测试 (6 cases) |

**核心代码** (`buildProxyEnv`):
- 无 credentials → 直接返回 `os.Environ()` (向后兼容)
- 有 credentials → 从 OS keychain 读取 secret，注入/覆盖对应环境变量
- 失败时返回明确错误（哪个 keychain key 找不到）

**用法**:

```bash
# 1. 存储 API key 到 OS keychain
harbor auth github-pat
# 输入: ghp_xxxxxxxxxxxx

# 2. proxy 自动从 keychain 读取，注入给 upstream
harbor proxy --credential GITHUB_TOKEN=github-pat \
  npx @modelcontextprotocol/server-github
```

**Claude Desktop / Claude Code 配置**:

```json
{
  "mcpServers": {
    "github": {
      "command": "harbor",
      "args": [
        "proxy",
        "--credential", "GITHUB_TOKEN=github-pat",
        "npx", "@modelcontextprotocol/server-github"
      ]
    }
  }
}
```

API key 存在 OS keychain 里，配置文件里只有 keychain key 名称。支持多个 `--credential` flag。

---

### 测试结果

全部测试通过:

```
ok  gateway              0.857s
ok  internal/cli         0.535s
ok  internal/connector   4.271s
ok  internal/memory      1.388s
ok  internal/metrics     1.597s
ok  internal/pipeline    1.814s
ok  internal/proxy      22.162s
ok  internal/recall      2.962s
ok  internal/registry    2.716s
ok  internal/schema      2.256s
ok  internal/tooling     2.010s
```

---

### Roadmap 更新

更新 roadmap 到 v5，整合竞品分析结果（17+ 产品调研）：
- 新增原则："做数据智能层，不做 gateway"
- 识别 IBM ContextForge 为主要竞品（最接近 Harbor 定位）
- Schema learning network effect 作为核心护城河
- Phase 4 细化为可执行的发布计划

---

### 未验证/待做

1. **跨 session 记忆** — `harbor_recall` 已实现，需要真实两个 Claude Code session 验证数据跨 session 召回
2. **Proxy 环境隔离** — 当前 credential 注入不做环境隔离（仍透传所有 env），完整隔离是 v2 feature
3. **权限粒度控制** — 用户提出的 "权限问题也很难管理"，当前只解决了 credential 注入，未解决细粒度权限（read-only scope 等）

### 文件清单

新增:
- `internal/proxy/e2e_test.go` — 端到端测试
- `internal/proxy/quality_test.go` — LLM 质量测试
- `internal/proxy/credential_test.go` — 凭证注入测试
- `internal/cli/proxy_test.go` — CLI flag 解析测试

修改:
- `internal/proxy/proxy.go` — Config.Credentials, buildProxyEnv(), env 注入
- `internal/proxy/handler.go` — buildLearnHint(), detectTopFields()
- `internal/cli/proxy.go` — extractCredentialFlags(), 帮助文本
- `.gitignore` — 新增 .claude/, .mcp.json, .gocache/, .harbor-sec/, tmp-harbor-check/
