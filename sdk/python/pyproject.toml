[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "harbor-sdk"
version = "0.1.0"
description = "Python SDK for building Harbor connectors"
license = {text = "Apache-2.0"}
requires-python = ">=3.10"
